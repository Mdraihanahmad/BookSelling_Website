{
  "name": "sellb-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "build": "npm run build --prefix client",
    "install:all": "npm ci --prefix server && npm ci --prefix client"
  }
}
